 <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
              <a class="navbar-brand" href="{{path('main')}}">Kingdom auto care</a>
              <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                  <li class="nav-item">
                    <a class="nav-link" href="#">Maps</a>
                  </li>
               
                  
                </ul>
                  <form class="d-flex" role="search">
                    <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                    <button class="btn btn-outline-success" type="submit">Search</button>
                  </form>

                <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {# verification de conxn de l'utilisateur #}
                {% if app.user %}
                {# is_granted is a gloabal twig function for verification of roles #}
                {% if is_granted('ROLE_PRODUCT_ADMIN') %}
                <li class="nav-item">
                    <a href="{{path('admin_index')}}" class="nav-link">Administration</a>
                  </li>
                {% endif %}
                
                   <li class="nav-item">
                    <a href="{{path('profile_index')}}" class="nav-link">Mon compte</a>
                  </li>
                  <li class="nav-item">
                    <a href="{{path('app_logout')}}" class="nav-link">Me déconnecter</a>
                  </li>
                {% endif %}
                 
                  {% if app.user is empty %}
                  <li class="nav-item">
                    <a href="{{path('app_login')}}" class="nav-link">Me connecter</a>
                  </li>
                  {% endif %}
                </ul>
                
              </div>
            </div>
          </nav>