{% extends 'base.html.twig' %}
{% block stylesheets %}
<link rel="stylesheet" href="{{asset('assets/css/index.css')}}"></link>
{% endblock %}

{% block title %}Kingdom auto-care{% endblock %}
{% block body %}
   <main class="container">
        <div class="row justify-content-around mt-4 ">
            <!-- categories -->
            <div class="bg-light background-dark col-4 col-sm-3 col-lg-2 d-none d-sm-block">
              <h1 >Categories</h1>
             <div class="row mt-3">
                      <!-- Kits Section -->
                      <h2 class="mt-2 m-auto medium titre">Lavage et Prélavage</h2>
                      <a href="{{ path('categories_list', {slug: 'prelavage'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Prélavage</a>
                      <a href="{{ path('categories_list', {slug: 'shampoing'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Shampoing</a>
                      <a href="{{ path('categories_list', {slug: 'gants'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Gants</a>
                      <a href="{{ path('categories_list', {slug: 'sechage'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Séchage</a>
                      <a href="{{ path('categories_list', {slug: 'goudron-calcaire'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Goudron & Calcaire</a>
                      <a href="{{ path('categories_list', {slug: 'demoustiquant'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Démoustiquant</a>
                      <a href="{{ path('categories_list', {slug: 'lavage-sans-eau'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Lavage Sans Eau</a>
                      <a href="{{ path('categories_list', {slug: 'peinture-mate'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Peinture Mate</a>
                      <a href="{{ path('categories_list', {slug: 'accessoires'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Accessoires</a>
              
                      <!-- Jantes et Pneus Section -->
                      <h2 class="mt-2 m-auto titre">Jantes et Pneus</h2>
                      <a href="{{ path('categories_list', {slug: 'decontaminants-ferreux'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Décontaminants ferreux</a>
                      <a href="{{ path('categories_list', {slug: 'nettoyant-jantes'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Nettoyant Jantes</a>
                      <a href="{{ path('categories_list', {slug: 'degraissant'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Dégraissant</a>
                      <a href="{{ path('categories_list', {slug: 'brosses-pinceaux'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Brosses & Pinceaux</a>
                      <a href="{{ path('categories_list', {slug: 'dressing-pneu'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Dressing Pneu</a>
                      <a href="{{ path('categories_list', {slug: 'protection-jantes'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Protection Jantes</a>
              
                      <!-- Décontamination Section -->
                      <h2 class="mt-2 m-auto medium titre">Décontamination</h2>
                      <a href="{{ path('categories_list', {slug: 'clay-bar'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Clay Bar</a>
                      <a href="{{ path('categories_list', {slug: 'lubrifiant'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Lubrifiant</a>
                      <a href="{{ path('categories_list', {slug: 'degoudronnant'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Dégoudronnant</a>
                      <a href="{{ path('categories_list', {slug: 'accessoires'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Accessoires</a>
              
                      <!-- Correction & Polissage Section -->
                      <h2 class="mt-2 m-auto medium titre">Correction & Polissage</h2>
                      <a href="{{ path('categories_list', {slug: 'polisseuses'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Polisseuses</a>
                      <a href="{{ path('categories_list', {slug: 'polish'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Polish</a>
                      <a href="{{ path('categories_list', {slug: 'pads-machines'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Pads Machines</a>
                      <a href="{{ path('categories_list', {slug: 'pads-manuels'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Pads Manuels</a>
                      <a href="{{ path('categories_list', {slug: 'cleaner'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Cleaner</a>
                      <a href="{{ path('categories_list', {slug: 'wetsanding'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Wetsanding</a>
                      <a href="{{ path('categories_list', {slug: 'accessoires'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Accessoires</a>
              
                      <!-- Protection & Cire Section -->
                      <h2 class="mt-2 m-auto medium titre">Protection & Cire</h2>
                      <a href="{{ path('categories_list', {slug: 'lustreur-cleaner'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Lustreur & Cleaner</a>
                      <a href="{{ path('categories_list', {slug: 'cire-naturelle'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Cire Naturelle</a>
                      <a href="{{ path('categories_list', {slug: 'cire-synthetique'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Cire Synthétique</a>
                      <a href="{{ path('categories_list', {slug: 'protection-ceramique'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Protection Céramique</a>
                      <a href="{{ path('categories_list', {slug: 'quick-detailer-wax'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Quick Detailer / Wax</a>
                      <a href="{{ path('categories_list', {slug: 'peinture-mate'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Peinture Mate</a>
                      <a href="{{ path('categories_list', {slug: 'dressing-plastique'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Dressing Plastique</a>
              
                      <!-- Interieur Section -->
                      <h2 class="mt-2 m-auto medium titre">Interieur</h2>
                      <a href="{{ path('categories_list', {slug: 'tissus-moquettes'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Tissus & Moquettes</a>
                      <a href="{{ path('categories_list', {slug: 'cuirs'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Cuirs</a>
                      <a href="{{ path('categories_list', {slug: 'plastiques'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Plastiques</a>
                      <a href="{{ path('categories_list', {slug: 'impermeabilisant-textile'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Imperméabilisant textile</a>
                      <a href="{{ path('categories_list', {slug: 'senteurs-destructeurs-odeurs'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Senteurs et Destructeurs d'odeurs</a>
              
                      <!-- Vitres Section -->
                      <h2 class="mt-2 m-auto medium titre">Vitres</h2>
                      <a href="{{ path('categories_list', {slug: 'polish-vitres-phares'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Polish Vitres & Phares</a>
                      <a href="{{ path('categories_list', {slug: 'nettoyant'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Nettoyant</a>
                      <a href="{{ path('categories_list', {slug: 'protection-vitres'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Protection Vitres</a>
                      <a href="{{ path('categories_list', {slug: 'microfibres-vitres'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Microfibres Vitres</a>
              
                      <!-- Accessoires Section -->
                      <h2 class="mt-2 m-auto medium titre">Accessoires</h2>
                      <a href="{{ path('categories_list', {slug: 'microfibres'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Microfibres</a>
                      <a href="{{ path('categories_list', {slug: 'applicateurs-pads-manuels'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Applicateurs & Pads Manuels</a>
                      <a href="{{ path('categories_list', {slug: 'pulverisateurs'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Pulvérisateurs</a>
                      <a href="{{ path('categories_list', {slug: 'brosses-pinceaux'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Brosses & Pinceaux</a>
                      <a href="{{ path('categories_list', {slug: 'detailing-bag'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Detailing bag</a>
                      <a href="{{ path('categories_list', {slug: 'autres'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Autres</a>
              
                      <!-- Autres Surfaces Section -->
                      <h2 class="mt-2 m-auto medium titre">Autres Surfaces</h2>
                      <a href="{{ path('categories_list', {slug: 'compartiment-moteur'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Compartiment Moteur</a>
                      <a href="{{ path('categories_list', {slug: 'cabriolet'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Cabriolet</a>
                      <a href="{{ path('categories_list', {slug: 'chromes-metaux'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Chromes & Métaux</a>
                      <a href="{{ path('categories_list', {slug: 'moto-scooter'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">Moto & Scooter</a>
                      <a href="{{ path('categories_list', {slug: 'ppf-covering'}) }}" class="col-12 mt-2 rounded text-decoration-none text-white bg-dark p-3 text-wrap">PPF - Covering</a>
                  </div>      
          </div>

            <!-- produits -->
            <div class="col-12 col-sm-9 carousel-container">

                <div class="row ">
                  <!-- carousel starts -->
                  <div id="carouselExampleRide" class="carousel slide" data-bs-ride="true">
                    <div class="carousel-inner">
                      <div class="carousel-item active">
                        <img src="{{ asset('assets/uploads/detailingshop-cover-1.png') }}" class="d-block w-100" alt="...">
                      </div>
                      <div class="carousel-item">
                        <img src="{{ asset('assets/uploads/detailingshop-cover.png') }}" class="d-block w-100" alt="...">
                      </div>
                      <div class="carousel-item">
                        <img src="{{ asset('assets/uploads/audi.jpg') }}" class="d-block w-100" alt="...">
                      </div>
                      
                    </div>
                    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleRide" data-bs-slide="prev">
                      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                      <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleRide" data-bs-slide="next">
                      <span class="carousel-control-next-icon" aria-hidden="true"></span>
                      <span class="visually-hidden">Next</span>
                    </button>
                  </div>
                  <!--carousel ends-->
                <strong class="promo mt-2" style="width: 97%; margin:0 auto 1rem auto;">PROMOTIONS !!!</strong>


                        {% for category in categories %}
                        {% if category.products is defined %}
                            {% for product in category.products %}
                            <div class="col-6 col-lg-4 col-md-6 col-sm-6   mb-4">
                              <div class="card card-height" >
                                  <img src="{{ asset('assets/images/products/'  ~ product.image.name ) }}" class="d-block image-height"  alt="">
                                <div class="card-body">
                                  
                                  <h4 class="card-title">{{product.name}}</h4>
                                  <p class="card-text">{{product.description}}</p>
                                </div>
                                <div class="card-footer" style="display: flex; justify-content: space-between; align-items: end;">
                                  <h5 class="prix">Prix :{{product.prix / 100}}$</h5>
                                  <a href="{{path('products_details', {slug:product.slug})}}" class="btn btn-primary btn-bloc">Voir le Produit</a>
                                </div>
                              </div>
                          </div>
                            {% endfor %}
                        {% endif %}

                        {% endfor %}




                  </div>

            </div>
        </div>

  </main>


{% endblock %}

